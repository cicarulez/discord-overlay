# Discord Service — Environment Variables Sample
# Copy this file to `.env` and fill in the values.

# HTTP port for the service (default: 5090)
PORT=5090

# Discord bot token (required)
DISCORD_BOT_TOKEN=your_bot_token_here

# Optional: Restrict operations to a specific guild
GUILD_ID=your_guild_id_optional

# Optional: Voice channel to observe; if set, members will be tracked from this channel
VOICE_CHANNEL_ID=your_voice_channel_id_optional

# --- Auth settings (JWT + .htpasswd) ---
# Secret used to sign JWT access tokens (REQUIRED to enable auth)
JWT_SECRET=change_me_strong_secret
# Token lifetime (e.g. 8h, 1d)
JWT_EXPIRES_IN=8h
# Path to an Apache-style .htpasswd file with bcrypt hashes
# Each line: username:hash
# Example hash generation (node):
#   const bcrypt=require('bcryptjs'); bcrypt.hashSync('yourPassword', 10)
HTPASSWD_PATH=./.htpasswd
